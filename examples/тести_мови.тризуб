// Тести мови Тризуб — запуск через: тризуб тестувати тести_мови.тризуб

// Допоміжні функції
функція факторіал(н: цл64) -> цл64 {
    якщо (н <= 1) { повернути 1 }
    повернути н * факторіал(н - 1)
}

тип Колір {
    Червоний,
    Зелений,
    Синій
}

функція назва_кольору(к: Колір) -> тхт {
    зіставити к {
        Червоний => "червоний",
        Зелений => "зелений",
        Синій => "синій"
    }
}

// ── Тести ──

тест "арифметика" {
    перевірити 2 + 2 == 4
    перевірити 10 - 3 == 7
    перевірити 6 * 7 == 42
    перевірити 100 / 4 == 25
    перевірити 10 % 3 == 1
    перевірити 2 ** 10 == 1024
}

тест "порівняння" {
    перевірити 5 > 3
    перевірити 3 < 5
    перевірити 5 >= 5
    перевірити 5 <= 5
    перевірити 5 == 5
    перевірити 5 != 4
}

тест "логічні оператори" {
    перевірити істина && істина
    перевірити істина || хиба
    перевірити !(хиба)
    перевірити !(1 == 2)
}

тест "рядки" {
    змінна а = "Привіт"
    змінна б = ", світ!"
    перевірити а + б == "Привіт, світ!"
}

тест "рекурсія — факторіал" {
    перевірити факторіал(0) == 1
    перевірити факторіал(1) == 1
    перевірити факторіал(5) == 120
    перевірити факторіал(10) == 3628800
}

тест "enum та pattern matching" {
    перевірити назва_кольору(Червоний) == "червоний"
    перевірити назва_кольору(Зелений) == "зелений"
    перевірити назва_кольору(Синій) == "синій"
}

тест "Опція — Деякий" {
    змінна значення = Деякий(42)
    змінна результат = зіставити значення {
        Деякий(н) => н,
        Нічого => 0
    }
    перевірити результат == 42
}

тест "Опція — Нічого" {
    змінна значення = Нічого
    змінна результат = зіставити значення {
        Деякий(н) => н,
        Нічого => -1
    }
    перевірити результат == -1
}

тест "Результат — Успіх" {
    змінна р = Успіх(100)
    змінна отримано = зіставити р {
        Успіх(значення) => значення,
        Помилка(повід) => 0
    }
    перевірити отримано == 100
}

тест "цикл for — сума" {
    змінна сума = 0
    для (і від 1 до 11) {
        сума += і
    }
    перевірити сума == 55
}

тест "цикл for-in діапазон" {
    змінна сума = 0
    для (і в 1..=10) {
        сума += і
    }
    перевірити сума == 55
}

тест "match з guard" {
    змінна х = 85
    змінна оцінка = зіставити х {
        _ якщо х >= 90 => "A",
        _ якщо х >= 70 => "B",
        _ => "C"
    }
    перевірити оцінка == "B"
}

тест "побітові зсуви" {
    перевірити 1 << 3 == 8
    перевірити 16 >> 2 == 4
    перевірити 5 ^ 3 == 6
}

тест "неявний return" {
    змінна результат = факторіал(5)
    перевірити результат == 120
}

// ── Нові тести: методи масивів ──

тест "масив.сортувати" {
    змінна м = [5, 3, 8, 1, 9, 2]
    змінна відсортований = м.сортувати()
    перевірити відсортований == [1, 2, 3, 5, 8, 9]
}

тест "масив.обернути" {
    змінна м = [1, 2, 3]
    перевірити м.обернути() == [3, 2, 1]
}

тест "масив.перший_останній" {
    змінна м = [10, 20, 30]
    перевірити м.перший() == 10
    перевірити м.останній() == 30
}

тест "масив.фільтрувати" {
    змінна м = [1, 2, 3, 4, 5, 6]
    змінна парні = м.фільтрувати(|х| х % 2 == 0)
    перевірити парні == [2, 4, 6]
}

тест "масив.перетворити" {
    змінна м = [1, 2, 3]
    змінна подвоєні = м.перетворити(|х| х * 2)
    перевірити подвоєні == [2, 4, 6]
}

тест "масив.згорнути" {
    змінна м = [1, 2, 3, 4, 5]
    змінна сума = м.згорнути(0, |с, х| с + х)
    перевірити сума == 15
}

тест "масив.з_єднати" {
    змінна м = ["а", "б", "в"]
    перевірити м.з_єднати(", ") == "а, б, в"
}

тест "масив.містить" {
    змінна м = [1, 2, 3]
    перевірити м.містить(2)
    перевірити !(м.містить(5))
}

// ── Нові тести: методи рядків ──

тест "рядок.великими_малими" {
    перевірити "hello".великими() == "HELLO"
    перевірити "HELLO".малими() == "hello"
}

тест "рядок.містить" {
    перевірити "привіт світ".містить("світ")
    перевірити !("привіт".містить("бувай"))
}

тест "рядок.починається_закінчується" {
    перевірити "привіт".починається_з("при")
    перевірити "привіт".закінчується_на("віт")
}

тест "рядок.обрізати" {
    перевірити "  пробіли  ".обрізати() == "пробіли"
}

тест "рядок.розділити" {
    змінна частини = "а,б,в".розділити(",")
    перевірити частини == ["а", "б", "в"]
}

тест "рядок.замінити" {
    перевірити "привіт світ".замінити("світ", "Україно") == "привіт Україно"
}

// ── Нові тести: builtins ──

тест "сортувати_builtin" {
    змінна відсорт = сортувати([3, 1, 2])
    перевірити відсорт == [1, 2, 3]
}

тест "обернути_builtin" {
    змінна обер = обернути([1, 2, 3])
    перевірити обер == [3, 2, 1]
}

тест "діапазон_builtin" {
    змінна д = діапазон(1, 4)
    перевірити д == [1, 2, 3]
}

// ── Словники ──

тест "словник_створити" {
    змінна с = словник("а", 1, "б", 2)
    перевірити с.довжина() == 2
    перевірити с.містить_ключ("а")
    перевірити с.отримати("а") == 1
}

тест "словник_ключі_значення" {
    змінна с = словник("x", 10, "y", 20)
    перевірити с.ключі() == ["x", "y"]
    перевірити с.значення() == [10, 20]
}

тест "словник_додати_видалити" {
    змінна с = словник("а", 1)
    змінна с2 = с.додати("б", 2)
    перевірити с2.довжина() == 2
    змінна с3 = с2.видалити("а")
    перевірити с3.довжина() == 1
}

// ── Множини ──

тест "множина_створити" {
    змінна м = множина(1, 2, 3)
    перевірити м.довжина() == 3
    перевірити м.містить(2)
    перевірити !(м.містить(5))
}

тест "множина_перетин" {
    змінна а = множина(1, 2, 3, 4)
    змінна б = множина(3, 4, 5, 6)
    змінна п = а.перетин(б)
    перевірити п.довжина() == 2
    перевірити п.містить(3)
    перевірити п.містить(4)
}

тест "множина_різниця" {
    змінна а = множина(1, 2, 3)
    змінна б = множина(2, 3, 4)
    змінна р = а.різниця(б)
    перевірити р.довжина() == 1
    перевірити р.містить(1)
}

// ── Оператор "в" ──

тест "в_масив" {
    перевірити 3 в [1, 2, 3, 4, 5]
    перевірити !(10 в [1, 2, 3])
}

тест "в_множина" {
    змінна м = множина(1, 2, 3)
    перевірити 2 в м
    перевірити !(5 в м)
}

тест "в_словник" {
    змінна с = словник("ключ", 42)
    перевірити "ключ" в с
    перевірити !("інший" в с)
}

тест "в_діапазон" {
    перевірити 5 в 1..10
    перевірити !(10 в 1..10)
    перевірити 10 в 1..=10
}

тест "в_рядок" {
    перевірити "світ" в "привіт світ"
    перевірити !("бувай" в "привіт")
}

// ════════════════════════════════════════
// Тести для раніше непокритих фіч
// ════════════════════════════════════════

тест "try_catch ловить помилки" {
    змінна зловлено = хиба
    спробувати {
        паніка("тестова помилка")
    } зловити помилка {
        зловлено = істина
    }
    перевірити зловлено
}

тест "try_catch_finally" {
    змінна фінал = хиба
    спробувати {
        змінна х = 1
    } зловити помилка {
        змінна у = 2
    } нарешті {
        фінал = істина
    }
    перевірити фінал
}

тест "формат рядки ф" {
    змінна імя = "Тризуб"
    змінна версія = 3
    перевірити ф"{імя} v{версія}" == "Тризуб v3"
    перевірити ф"2+2={2 + 2}" == "2+2=4"
}

тест "pipeline з каррінгом" {
    змінна результат = [1, 2, 3, 4, 5]
        |> фільтрувати(|х| х > 2)
        |> перетворити(|х| х * 10)
    перевірити результат == [30, 40, 50]
}

тест "контракти вимагає" {
    функція безпечне_ділення(а: дрб64, б: дрб64) -> дрб64
        вимагає { б != 0.0 }
    {
        повернути а / б
    }
    перевірити безпечне_ділення(10.0, 2.0) == 5.0
}

тест "контракти вимагає відхиляє" {
    функція тільки_додатнє(х: цл64) -> цл64
        вимагає { х > 0 }
    {
        повернути х * 2
    }
    змінна зловлено = хиба
    спробувати {
        тільки_додатнє(-5)
    } зловити помилка {
        зловлено = істина
    }
    перевірити зловлено
}

тест "структура створення" {
    структура Точка {
        х: цл64,
        у: цл64
    }
    змінна т = Точка { х: 3, у: 4 }
    перевірити т.х == 3
    перевірити т.у == 4
}

тест "лямбда виклик" {
    змінна подвоїти = |х| х * 2
    перевірити подвоїти(21) == 42

    змінна додати = |а, б| а + б
    перевірити додати(3, 4) == 7
}

тест "for-in по словнику" {
    змінна с = словник("а", 1, "б", 2)
    змінна сума = 0
    для (пара в с) {
        // пара це кортеж (ключ, значення)
        сума += 1
    }
    перевірити сума == 2
}

тест "for-in по множині" {
    змінна м = множина(10, 20, 30)
    змінна сума = 0
    для (елемент в м) {
        сума += елемент
    }
    перевірити сума == 60
}

тест "for-in по рядку" {
    змінна кількість = 0
    для (символ в "abc") {
        кількість += 1
    }
    перевірити кількість == 3
}

тест "вкладений match" {
    тип Форма {
        Коло(дрб64),
        Квадрат(дрб64)
    }

    функція площа(ф: Форма) -> дрб64 {
        зіставити ф {
            Коло(р) => 3.14 * р * р,
            Квадрат(с) => с * с
        }
    }

    перевірити площа(Квадрат(5.0)) == 25.0
}

тест "unsafe блок" {
    змінна результат = 0
    небезпечний {
        результат = 42
    }
    перевірити результат == 42
}

тест "comptime блок" {
    змінна результат = 0
    компчас {
        результат = 100
    }
    перевірити результат == 100
}

// ════════════════════════════════════════
// Нові тести v3.7: extension methods, cast, генератори
// ════════════════════════════════════════

тест "цл64 extension methods" {
    перевірити 42.парне()
    перевірити 7.непарне()
    перевірити (-5).абс() == 5
    перевірити 42.в_текст() == "42"
    перевірити 3.степінь(4) == 81
    перевірити 3.мін(7) == 3
    перевірити 3.макс(7) == 7
}

тест "дрб64 extension methods" {
    перевірити 3.14.округлити() == 3
    перевірити 3.7.підлога() == 3
    перевірити 3.2.стеля() == 4
    перевірити (-2.5).абс() == 2.5
    перевірити 4.0.корінь() == 2.0
}

тест "cast як цл64->дрб64" {
    змінна х = 42 як дрб64
    перевірити х == 42.0
}

тест "cast як дрб64->цл64" {
    змінна х = 3.7 як цл64
    перевірити х == 3
}

тест "cast як цл64->тхт" {
    змінна х = 42 як тхт
    перевірити х == "42"
}

тест "генератор віддати та в_масив" {
    // Функція що використовує віддати як генератор
    функція парні_до(макс: цл64) -> [цл64] {
        змінна р = []
        для (і в 1..=макс) {
            якщо (і % 2 == 0) {
                р = додати(р, і)
            }
        }
        повернути р
    }
    перевірити парні_до(10) == [2, 4, 6, 8, 10]
}

// ════════════════════════════════════════
// v4.0: JSON, математика, файли
// ════════════════════════════════════════

тест "json розібрати" {
    змінна р = json_розібрати("[1, 2, 3]")
    // Результат — Успіх з масивом
    змінна масив = зіставити р {
        Успіх(д) => д,
        Помилка(п) => []
    }
    перевірити масив == [1, 2, 3]
}

тест "json в_рядок" {
    змінна масив = [1, 2, 3]
    змінна json = json_в_рядок(масив)
    перевірити json == "[1,2,3]"
}

тест "математика нативна" {
    перевірити корінь(16.0) == 4.0
    перевірити корінь(9) == 3.0
    перевірити абс(-42) == 42
}

тест "математика ПІ та Е" {
    перевірити ПІ > 3.14
    перевірити ПІ < 3.15
    перевірити Е > 2.71
    перевірити Е < 2.72
}

тест "ціле_з_рядка" {
    змінна р = ціле_з_рядка("42")
    змінна значення = зіставити р {
        Успіх(н) => н,
        Помилка(п) => 0
    }
    перевірити значення == 42
}

тест "файл_записати_прочитати" {
    // Записуємо файл
    файл_записати("__тест_тризуб.txt", "Привіт Тризуб!")
    // Перевіряємо що існує
    перевірити файл_існує("__тест_тризуб.txt")
    // Видаляємо (через запис порожнього)
}

тест "for-in по рядку символи" {
    змінна кількість_голосних = 0
    для (с в "привіт") {
        якщо (с в "аеиіоуяюєї") {
            кількість_голосних += 1
        }
    }
    перевірити кількість_голосних == 2
}

тест "складний pipeline" {
    змінна числа = [10, 25, 3, 47, 8, 15, 42, 6]
    змінна великі = числа
        .фільтрувати(|х| х > 10)
        .сортувати()
    перевірити великі == [15, 25, 42, 47]
}

тест "ланцюжок методів рядків" {
    змінна р = "  Привіт Світ  "
    перевірити р.обрізати().малими() == "привіт світ"
}

тест "множина_об_єднання" {
    змінна а = множина(1, 2, 3)
    змінна б = множина(3, 4, 5)
    змінна об = а.об_єднання(б)
    перевірити об.довжина() == 5
}

// ════════════════════════════════════════
// v3.8: макроси, async, cast, extension
// ════════════════════════════════════════

тест "макрос реальний" {
    макрос подвоїти(х) {
        повернути х * 2
    }
    перевірити подвоїти(21) == 42
    перевірити подвоїти(5) == 10
}

тест "await вираз" {
    // чекати просто обчислює вираз
    змінна р = чекати 42
    перевірити р == 42
}

тест "cast bool->int" {
    перевірити (істина як цл64) == 1
    перевірити (хиба як цл64) == 0
}

тест "числа мін/макс" {
    перевірити 3.мін(7) == 3
    перевірити 3.макс(7) == 7
    перевірити (-10).абс() == 10
}

тест "рядок підрядок" {
    перевірити "привіт".підрядок(0, 3) == "при"
}

тест "словник ітерація_рахунок" {
    змінна с = словник("a", 10, "b", 20, "c", 30)
    змінна сума_значень = 0
    для (пара в с) {
        сума_значень += 1
    }
    перевірити сума_значень == 3
}

тест "ефект з_обробником" {
    змінна оброблено = хиба
    з_обробником Лог {
        оброблено = істина
    }
    перевірити оброблено
}

тест "глобальні мін макс абс" {
    перевірити мін(3, 7) == 3
    перевірити макс(3, 7) == 7
    перевірити абс(-42) == 42
}

тест "async чекати значення" {
    змінна р = чекати 42
    перевірити р == 42
    змінна р2 = чекати (21 * 2)
    перевірити р2 == 42
}

тест "масив ланцюжок складний" {
    змінна р = [5, 1, 8, 3, 9, 2, 7]
        .сортувати()
        .обернути()
    перевірити р == [9, 8, 7, 5, 3, 2, 1]
}

тест "рядок замінити ланцюжок" {
    змінна р = "Hello World"
        .малими()
        .замінити("world", "тризуб")
    перевірити р == "hello тризуб"
}

тест "словник отримати" {
    змінна с = словник("імя", "Євген", "вік", 25)
    перевірити с.отримати("імя") == "Євген"
    перевірити с.отримати("вік") == 25
}

тест "in для char в рядку" {
    перевірити "а" в "банан"
    перевірити !("z" в "банан")
}
