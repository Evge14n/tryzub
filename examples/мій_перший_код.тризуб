// Мій перший код на Тризуб
// Проста програма для початківців

функція головна() {
    // Привітання
    друк("🇺🇦 Вітаємо у світі програмування Тризуб!")
    друк("=====================================")
    
    // Змінні
    змінна ім'я = "Євген"
    змінна вік = 25
    змінна місто = "Тернопіль"
    
    // Виведення
    друк("Привіт, мене звуть " + ім'я)
    друк("Мені " + ціле_в_рядок(вік) + " років")
    друк("Я живу в місті " + місто)
    
    // Арифметика
    друк("\nТрохи математики:")
    змінна а = 10
    змінна б = 3
    друк(ціле_в_рядок(а) + " + " + ціле_в_рядок(б) + " = " + ціле_в_рядок(а + б))
    друк(ціле_в_рядок(а) + " - " + ціле_в_рядок(б) + " = " + ціле_в_рядок(а - б))
    друк(ціле_в_рядок(а) + " * " + ціле_в_рядок(б) + " = " + ціле_в_рядок(а * б))
    друк(ціле_в_рядок(а) + " / " + ціле_в_рядок(б) + " = " + ціле_в_рядок(а / б))
    
    // Умовні оператори
    друк("\nПеревірка віку:")
    якщо (вік >= 18) {
        друк("Ви повнолітній!")
    } інакше {
        друк("Ви ще не повнолітній")
    }
    
    // Цикли
    друк("\nЛічба від 1 до 5:")
    для (і від 1 до 6) {
        друк("Число: " + ціле_в_рядок(і))
    }
    
    // Масиви
    змінна дні = ["Понеділок", "Вівторок", "Середа", "Четвер", "П'ятниця"]
    друк("\nРобочі дні тижня:")
    для (день в дні) {
        друк("- " + день)
    }
    
    друк("\n✨ Вітаємо! Ви щойно запустили свою першу програму на Тризуб!")
}
